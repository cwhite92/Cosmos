function Enemy(x, y) {
    // Create the enemy sprite
    this.sprite = new Image();
    this.sprite.src = '../img/player.png';

    // The enemies co-ordinates
    this.x = x;
    this.y = y;
}

Enemy.prototype.update = function() {

}

enemy.prototype.draw = function() {

}